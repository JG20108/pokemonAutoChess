import { ArraySchema, MapSchema, Schema, SetSchema } from '@colyseus/schema';
import BotManager from '../../core/bot-manager';
import Simulation from '../../core/simulation';
import { FloatingItem } from '../../models/colyseus-models/floating-item';
import Player from '../../models/colyseus-models/player';
import { PokemonAvatarModel } from '../../models/colyseus-models/pokemon-avatar';
import { Portal, SynergySymbol } from '../../models/colyseus-models/portal';
import Shop from '../../models/shop';
import { EloRank } from '../../types/Config';
import { GameMode, GamePhaseState } from '../../types/enum/Game';
import { Item } from '../../types/enum/Item';
import { Pkm } from '../../types/enum/Pokemon';
import { SpecialGameRule } from '../../types/enum/SpecialGameRule';
import { Weather } from '../../types/enum/Weather';
import { TownEncounter } from '../../core/town-encounters';
interface GameStateOptions {
    preparationId: string;
    name: string;
    noElo: boolean;
    minRank: EloRank | null;
    maxRank: EloRank | null;
    weather: Weather;
    phase: GamePhaseState;
    PICK_PHASE_DURATION: number;
    status: any;
}
export default class GameState extends Schema {
    afterGameId: string;
    roundTime: number;
    phase: GamePhaseState;
    players: MapSchema<Player, string>;
    avatars: MapSchema<PokemonAvatarModel, string>;
    floatingItems: MapSchema<FloatingItem, string>;
    portals: MapSchema<Portal, string>;
    symbols: MapSchema<SynergySymbol, string>;
    additionalPokemons: ArraySchema<Pkm>;
    stageLevel: number;
    weather: Weather;
    noElo: boolean;
    gameMode: GameMode;
    spectators: SetSchema<string>;
    simulations: MapSchema<Simulation, string>;
    lightX: number;
    lightY: number;
    specialGameRule: SpecialGameRule | null;
    townEncounter: TownEncounter | null;
    time: number;
    updatePhaseNeeded: boolean;
    gameFinished: boolean;
    gameLoaded: boolean;
    name: string;
    startTime: number;
    endTime: number | undefined;
    preparationId: string;
    shinyEncounter: boolean;
    seed: number;
    turn: number;
    phaseStartedAt: number;
    phaseEndedAt: number;
    history: string[];
    shopLocked: boolean;
    levelCost: number;
    levelMaxExp: number;
    expPerBattle: number;
    expPerBuy: number;
    rerollCost: number;
    pokemonPrices: number[];
    pokemonPerShop: number;
    maxLevel: number;
    maxPokemonPerTeam: number;
    maxItemPerPokemon: number;
    maxCopiesPerPokemon: number;
    maxCopiesPerItem: number;
    maxTeamCost: number;
    maxLife: number;
    maxMana: number;
    boosterDropRate: number;
    shinyRate: number;
    weatherDuration: number;
    weatherStartedAt: number;
    status: string;
    statusStartedAt: number;
    statusEndedAt: number;
    statusMessage: string;
    statusMessageStartedAt: number;
    statusMessageEndedAt: number;
    minRank: EloRank | null;
    maxRank: EloRank | null;
    botManager: BotManager;
    shop: Shop;
    townEncounters: Set<TownEncounter>;
    pveRewards: Item[];
    pveRewardsPropositions: Item[];
    wanderers: Map<string, Pkm>;
    constructor(options: GameStateOptions, players: Player[], gameMode: GameMode, specialGameRule?: SpecialGameRule, seed?: string);
}
export {};
